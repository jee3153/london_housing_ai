import { TabContainer } from "./TabContainer";

export default function DashboardPage() {
    return (
        <div className="min-h-screen flex flex-col">
            <TabContainer />
            {/* You may wrap the content or include header/nav above as needed */}
        </div>
    );
}
